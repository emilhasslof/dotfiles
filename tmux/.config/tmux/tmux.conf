set -g base-index 1

set-option -g history-limit 25000
set -g mouse on

set -sg escape-time 1

# vi for copy mode
setw -g mode-keys vi

# Set the prefix to Ctrl-j
unbind C-b
set -g prefix C-j
bind C-j send-prefix

# Reload the file
bind r source-file ~/.config/tmux/tmux.conf \; display "Config reloaded!"

# status bar
# pomo is optional: only shown if the `pomo` binary is installed.
set -g status-right "#(command -v pomo >/dev/null && pomo)"
set -g status-style "fg=#665c54"
#set -g status-left-style "fg=#928374"

set -g status-bg default
set -g status-position top
set -g status-interval 3
set -g status-left ""

# Clipboard: pbcopy is a portable shim (~/.local/bin/pbcopy) that maps to
# wl-copy / xclip / native pbcopy depending on the platform.
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
bind P paste-buffer
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"

bind-key j command-prompt -p "join pane from:"  "join-pane -s '%%'"
bind-key b "break-pane"
bind c "new-window -c '#{pane_current_path}'"
bind '"' " split-window -v -c '#{pane_current_path}'"
bind % " split-window -h -c '#{pane_current_path}'"
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
